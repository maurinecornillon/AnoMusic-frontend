// Import the createContext function from the "react" library
import { createContext } from "react";

// Create a new context called "UserContext" using the createContext function
const UserContext = createContext();


// Export the UserContext to make it available for use in other parts of the application
export default UserContext;
